name: Github-CI
run-name: Continuous Integration Test
on: [push]
jobs:
  Run-tests:
    runs-on: self-hosted
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: "20"
      - run: npm ci
      - run: npm run test
